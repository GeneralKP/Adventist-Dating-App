import React from 'react';
import {StyleSheet, Text, View} from 'react-native';
import ApolloProviderComponent from './src/config';

const App = () => {
  return <ApolloProviderComponent />;
};

const styles = StyleSheet.create({});

export default App;
